export enum JSON_EDITOR_MODE {
  CODE = 'code',
  TEXT = 'text',
  TREE = 'tree',
  VIEW = 'view'
}

export enum JSON_EDITOR_HEIGHT_UNIT {
  PIXELS = 'px',
  PERCENTAGE = '%',
  REM = 'rem'
}

export enum JSON_EDITOR_TYPE {
  ARRAY = 'array',
  ARRAY_OBJECT = 'array_object',
  OBJECT = 'object'
}