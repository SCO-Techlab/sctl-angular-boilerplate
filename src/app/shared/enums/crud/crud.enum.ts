export enum CRUD_COLUMN_TYPE {
  TEXT = 'text',
  NUMBER = 'number',
  BOOLEAN = 'boolean',
  DATE = 'date',
  AVATAR = 'avatar',
}

export enum CRUD_INPUT_TYPE {
  TEXT = 'text',
  NUMBER = 'number',
  BOOLEAN = 'boolean',
  DATE = 'date',
  PASSWORD = 'password',
  ENUM = 'enum',
  SELECT = 'select',
}